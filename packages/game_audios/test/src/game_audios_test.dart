// ignore_for_file: prefer_const_constructors
import 'package:flutter_test/flutter_test.dart';
import 'package:game_audios/game_audios.dart';

void main() {
  group('GameAudios', () {
    test('can be instantiated', () {
      expect(GameAudios(), isNotNull);
    });
  });
}
